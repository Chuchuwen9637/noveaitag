<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nanjie.mapper.AuthortagMapper">
<select id="selectallurl" resultType="java.lang.String">
    select url from authortag ORDER BY id desc
</select>
    <select id="select_all_authtag" resultType="com.nanjie.entity.Authortag">
        SELECT * FROM authortag ORDER BY id desc
    </select>
</mapper>
